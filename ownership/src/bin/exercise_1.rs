// 🌟🌟
fn main() {
    // Use as many approaches as you can to make it work
    let x = String::from("hello, world");
    let y = &x;
    // let y = x.clone(); (other approach)
    println!("{},{}", x, y);
}
